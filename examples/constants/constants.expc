haskell {

}


component counter() {
    input enable : Unsigned 1
    input interval : Unsigned 24
    state count = 0 : Unsigned 24
    output out : Unsigned 24

    count' = if enable == 1
        then count + interval
        else count
    
    out = count
}